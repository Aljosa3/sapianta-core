[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sapianta-core"
version = "1.3.0"
description = "Deterministic Governance Runtime (Layers 0-2)"
authors = [{ name = "SAPIANTA" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["sapianta_core*"]
