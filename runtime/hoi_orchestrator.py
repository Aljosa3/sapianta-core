# PATH: runtime/hoi_orchestrator.py

def hoi_orchestrator(input_payload: dict) -> dict:
    """
    HOI Orchestrator v0.2 — reference-only.
    No decisions. No execution.
    """

    return {
        "mode": "REFERENCE_RESPONSE",
        "scope": "HOI_v0.2",
        "content": {},
    }
