# HDS JSON-safe Output v0.3 — INIT

STATUS: INIT
SCOPE: New module only (no modification of LOCKED components)

## Invariants
- HOI has absolute legitimacy priority.
- HDS never decides.
- LLM is subordinate language executor without authority.
- Audit Harness is passive (stdout capture).

## Objective
Introduce a deterministic, structured, JSON-serializable HDS output (v0.3) with an Explain-on-Demand JSON equivalent.

## Constraints
- No signal detection
- No adaptation
- No memory
- No ranking
- No implicit authority
- HOI unchanged
- Audit Harness consumes stdout without interpretation

## Deliverables
- Separate module: hds_json_output_v0_3
- Deterministic JSON schema
- Minimal Python implementation
- CLI entrypoint emitting JSON to stdout

## Compatibility
- Coexists with HDS Chat Shell v0.1 / v0.2 (LOCKED)
- Compatible with Audit Harness v0.1 (stdout capture)

## Lock Statement
This INIT does not alter any LOCKED module. All changes are additive.

INIT COMPLETE.
